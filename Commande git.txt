// initialisation de Git
git init
// Liaison entre le projet et le Git
git remote add origin https://github.com/BouainTakoua/framework-selenium-cucumber.git
// Afficher statut
git status
// Ajouter les fichiers 
git add .
// Commit
git commit -m "first commit"
// Configurer l'adresse email
git cinfig --global user.email "bouaine_takoua@yahoo.fr"
// Configurer username
git cinfig --global user.name "BouainTakoua"
// Commit
git commit -m "first commit"
// pusher le code -f pour forcer -u au cas ou il n'y a aucun prob
git push -f origin
// ajouter un fichier gitignore
touch .gitignore